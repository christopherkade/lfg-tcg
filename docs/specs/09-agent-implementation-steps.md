# 9. AI Agent Implementation Steps

```markdown
1. Mount foundational project libraries: `@supabase/supabase-js`, `@supabase/ssr`, `framer-motion`, `lucide-react`.
2. Add `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` to `.env.local`, and create `src/lib/supabase/client.ts` (browser) and `src/lib/supabase/server.ts` (server, cookie-based) using `@supabase/ssr`.
3. Execute the schema + RLS + Realtime SQL from Section 3 (`supabase/schema.sql`) in the Supabase SQL Editor.
4. Scaffold core database types directly matching properties outlined in Section 3.
5. Add `proxy.ts` for optimistic auth redirects and `/auth/callback/route.ts` for the Discord OAuth code exchange, per Section 2.
6. Build the `(app)` route group with a shared layout rendering `TabBar` (mobile bottom nav / desktop top navbar) around three routes: `/` (LFG), `/pods` (Active Pods), `/profile` (Profile). `/login` and `/auth/callback` stay outside this group.
7. Code the Profile screen (`/profile/page.tsx`) as an identity-only form (username, discord handle) plus a destructive Sign Out button, per Section 5.
8. Code the LFG tab's Search dialog (`LfgDialog`), pre-filled from the profile's last-used settings (game, format, match type, location, playstyle, multi-select power brackets, players needed), wiring the conditional Framer Motion bracket picker to the MTG check.
9. Build dynamic UI components leveraging Tailwind values inherited dynamically from `GAMES_CONFIG[game].themeColor`.
10. Implement the pod lifecycle as Server Actions: `createPod` takes the dialog's field values, persists them onto `profiles.preferred_*`, and snapshots them into a new `pods` row; `cancelPod`/`markPodMatched` operate by id. Implement the join-request flow (request/accept/reject), per Sections 5 and 6.
11. Bind real-time reactive data hooks matching data parameter strings directly into the Postgres engine layers.
12. Add `app/manifest.ts` and icons per Section 7.
13. Add the optional `notes` field to the Search dialog and `pods` table; add `PodDetailDialog` (opened by clicking a Match Feed card) per Section 5; add join-request alerting (native `Notification` + in-app snackbar on new join requests, mounted in the `(app)` layout) per Sections 5 and 7.
14. Add per-member **Copy Handle** / **Copy All Handles** buttons to the Group Members list, and a **Mark as Matched** button that opens `ConfirmMarkMatchedDialog` (a warning reminding the host to add everyone on Discord first) before `markPodMatched` actually runs, per Section 5 and Section 8's Discord API constraint.
15. Extend the join-request alerting with a `MatchedDialog`, shown to each accepted member (not the host) when their pod transitions to `MATCHED`, per Section 5's Join Request Flow step 6.
16. Add the persisted `notifications` table + `notification_type` enum + SECURITY DEFINER triggers (`notify_on_pod_join_insert/update/delete`, `notify_on_pod_update`, `notify_on_pod_destroyed`) per Section 3, covering join request / accepted / rejected / member-left / pod-updated / pod-destroyed. Build `NotificationBell` (unread badge, dropdown, toast + native notification on new inserts, realtime-subscribed with no server-side `filter` — RLS's `recipient_id = auth.uid()` check alone scopes each subscriber to their own rows, matching every other realtime subscription in this codebase) and mount it in `TabBar`'s desktop navbar plus a new mobile top bar. Narrow the former `JoinRequestNotifier` down to just the `MatchedDialog` watcher and rename it `MatchedPodWatcher`, since `NotificationBell` now owns the join-request/accepted/rejected alerts via the persisted table instead of listening to `pod_joins`/`pods` directly.
17. Add `profiles.city` (Section 3) plus the config-driven `constants/citiesConfig.ts` (`CITIES_CONFIG`/`CITY_MAP`, Section 4) and its `CitySelector` autocomplete on the Profile screen (Section 5). Add `pods.city`, snapshotted from `profiles.city` by `createPod`/`updatePod` the same way every other `preferred_*` field is snapshotted. Wire the always-on city scoping into `MatchFeed`'s query (Section 6): Online pods always show, IRL pods are restricted to the viewer's own city.
18. Add `pods.scheduled_at` (Section 3) and the Search dialog's Date/Time picker fields, required for IRL and validated in `validateStartSearchInput` (Section 5's Screen 2); display it (falling back to `created_at` for ONLINE pods) on Match Feed cards and in `PodDetailDialog`'s "When" row.
19. Add the adjustable `PodFilters` bar (`/components/PodFilters.tsx`) above the Match Feed — Game, Match Type, Format, Date, and Power Bracket, per Section 5's Screen 3 and Section 6 — seeded from the viewer's profile but independently adjustable, with a "Clear all" reset to the neutral `NEUTRAL_POD_FILTERS` state.
20. Add a **Cancel Request**/**Leave** action to `PodDetailDialog` for the searcher's own join (`leavePod`, Section 5's Join Request Flow step 5), and per-notification delete plus a "Clear all" action to `NotificationBell` (`deleteNotification`/`deleteAllNotifications`, Section 7), backed by the `notifications` DELETE RLS policy (Section 3).
21. Add `TutorialDialog` (a question-mark icon button opening a step-by-step "how it works" modal) next to `NotificationBell` in both of `TabBar`'s header rows, per Section 5's Navigation Shell.
22. Add shareable pod links, per Section 5's Screen 3: extract `/pods/page.tsx`'s own-pod fetch + layout into a shared `PodsView` server component, add the `/pods/[id]` dynamic route (this app's first) that fetches one pod by id and passes it down, add a **Copy Link** button to My Pod Panel, and add `MatchFeed`'s `initialSharedPod` prop / "pinned pod" handling so a linked pod auto-opens in `PodDetailDialog` and stays live-updated without being injected into the visible feed.
23. Thread a `next` return-to-destination through the login flow, per Section 2, so a logged-out visit to a shared pod link isn't lost: `proxy.ts` and `requireUser`/`requireProfile` (`src/lib/session.ts`) append `?next=<path>` onto their `/login` redirects; split `/login/page.tsx` into a Server Component reading `searchParams.next` and a client `LoginView` that carries it onto `signInWithOAuth`'s `redirectTo`; the existing `/auth/callback/route.ts` already redirected to `next` and needed no change.
24. Add the `sweep-inactive-active-pods` `pg_cron` job (`supabase/sql/inactive_pod_cleanup.sql`) per Section 3's Inactive Pod Cleanup subsection, deleting `ACTIVE` pods idle for 12+ hours and notifying the host plus any `ACCEPTED` members via a new `POD_EXPIRED_INACTIVITY` notification type. Add the type to `notification_type` and `src/types/database.ts`'s `NotificationType`, and wire its icon/copy into `NotificationBell`'s `TYPE_ICON`/`describeNotification`.
25. Add the app-wide light/dark theme toggle, per Section 1's Theming subsection: `createAppTheme(mode)` (`src/lib/mui/theme.ts`) already supported both modes, so add the `pm_theme` cookie infra (`src/lib/theme/index.ts`, `server.ts`, `ThemeModeContext.tsx`, mirroring the locale cookie pattern), make `ThemeRegistry` build its MUI theme from `useThemeMode()` instead of a static dark theme, and add `ThemeToggle` (sun/moon `ToggleButtonGroup`) to `SettingsDialog`. Remove `LfgDialog`'s forced light-only `ThemeProvider` wrap and convert its (and `TabBar`'s and `PowerBracketPicker`'s) hardcoded Tailwind/hex colors to theme tokens so they switch with the rest of the app.
26. Add the "Past Pods" history feature, per Section 3's `pod_history` schema/RLS/trigger and Section 5's Screen 4: add `pod_history` plus the `snapshot_pod_history` SECURITY DEFINER trigger (`supabase/sql/pod_history.sql`) that snapshots a pod and its accepted-member roster into a durable row on the `ACTIVE -> MATCHED` transition, before the Matched/Expired Pod Cleanup sweep can hard-delete the source rows. Add the `PodHistoryEntry`/`PodHistoryMember` types (`src/types/database.ts`), a fourth `TabBar` tab (`History` icon, ordered Pods/LFG/History/Profile) and a `/history` route (`src/app/(app)/history/page.tsx`) listing every `MATCHED` pod the viewer hosted or played in, with no realtime and no pagination control.
27. Add a per-viewer delete to the Past Pods list, per Section 5's Screen 4: `hidden_by uuid[]` on `pod_history` plus the `hide_pod_history_entry` SECURITY DEFINER RPC (`supabase/sql/pod_history.sql`) let a viewer soft-delete an entry from their own list without erasing it for the host/other members it's shared with. Split the previously server-only history page into a server fetch (`page.tsx`) plus a client `HistoryList` component (`src/components/HistoryList.tsx`) that optimistically removes a card and fires `deletePodHistoryEntry` (`src/app/actions/history.ts`), mirroring `NotificationBell`'s delete pattern.
28. Add a first light-gamification stat, per Section 3 and Section 5's Screen 4: `get_games_played_count()` SECURITY DEFINER RPC (`supabase/sql/pod_history.sql`) counting every `pod_history` row the caller appears in as host or member, ignoring `hidden_by` so the count doesn't shrink when an entry is deleted from the viewer's own Past Pods list. Render it as a "Games played: N" line on `/history/page.tsx`, above the Past Pods list.
29. Add the `POD_UPDATED_PENDING` notification type per Section 3/Section 5's Join Request Flow step 7: extend `notification_type` and add `notify_pending_joiners_on_pod_update`, a SECURITY DEFINER trigger on `pods` (`supabase/sql/notify_pending_joiners_on_pod_update.sql`) that fires alongside the existing (untracked) `notify_on_pod_update` on the same `ACTIVE -> ACTIVE` detail-edit condition, but fans out to every `PENDING` `pod_joins` row instead of `ACCEPTED` ones. Add the type to `src/types/database.ts`'s `NotificationType` and wire its icon/copy into `NotificationBell`'s `TYPE_ICON`/`describeNotification` plus both locale dictionaries (`notification.podUpdatedPending`), phrased around "requested to join" rather than "joined" since the recipient isn't a member yet.
30. Add draft Terms of Service / Privacy Policy pages, per Section 2's Legal subsection: `src/lib/legal/content.ts` (EN/FR section arrays, deliberately outside the flat i18n dictionary convention), the shared `src/components/LegalPage.tsx` renderer, and the `/terms`/`/privacy` routes (added to `PUBLIC_PATHS` in `src/proxy.ts`). Link both from `SettingsDialog` and from `LoginView`'s footer.
31. Add DB-level abuse/spam guardrails, per Section 3's new callout: `profiles.last_pod_created_at`/`last_join_request_at` plus `enforce_pod_creation_cooldown`/`enforce_join_request_cooldown` (`BEFORE INSERT` triggers on `pods`/`pod_joins`, `supabase/sql/rate_limits.sql`) enforcing a 15s/10s per-user cooldown. Catch the raised `RATE_LIMITED_POD_CREATE`/`RATE_LIMITED_JOIN_REQUEST` errors in `createPod`/`requestJoin` and surface translated messages instead.
32. Add account deletion, per Section 3's new callout and Section 5's Screen 1: the `delete_own_account()` SECURITY DEFINER RPC (`supabase/sql/account_deletion.sql`) anonymizes the caller's entries in other users' `pod_history.members` snapshots, then deletes their `auth.users` row (cascading through the existing FK chain). Add the `deleteAccount()` server action (`src/app/actions/profile.ts`), `DeleteAccountDialog` (mirroring `ConfirmMarkMatchedDialog`'s shape with a destructive confirm button), and wire a "Delete Account" button into `ProfileForm` below Sign Out.
33. Add a click-to-open detail view for Past Pods, per Section 5's Screen 4: `PodHistoryDetailDialog` (`src/components/PodHistoryDetailDialog.tsx`), mirroring `PodDetailDialog`'s overlay/layout for a `PodHistoryEntryWithHost` (no `notes`/`max_players`, a flat host/members shape instead of live `pod_joins`) with its Join/Leave/Request footer replaced by Close + Delete. Wire it into `HistoryEntriesList`: clicking a card opens the dialog, and the existing inline trash-icon button gets `event.stopPropagation()` so it keeps working standalone. Add the `podDetailDialog.playersCountTotal` key (both locale dictionaries) for the dialog's total-participant chip.
34. Redesign the Profile screen's layout, per Section 5's Screen 1: add `ProfileHeader` (avatar left, username + read-only `discord_handle` stacked right, in a card) and `ProfileStats` (a 2-column stat card grid — the real `get_games_played_count()`-backed "Games Played" card plus dashed-border/muted "Win Rate"/"Current Streak" placeholders — and a full-width dashed placeholder card reserving space for future trends graphs), both above `ProfileForm`. Remove the now-redundant disabled `discord_handle` `TextField` from `ProfileForm` itself. Fetch `gamesPlayedCount` in `profile/page.tsx` via `Promise.all` alongside `getProfile`. Add Framer Motion entrance animation (header fade/slide-in, stat-card stagger, Games Played count-up), all gated by `useReducedMotion()`.
35. Move username editing into `ProfileHeader` as an inline pencil-icon-triggered field (Save/Cancel icon buttons), per Section 5's Screen 1, removing the standalone `username` `TextField` from `ProfileForm`. Split the old combined `upsertProfile` server action into `updateUsername` (insert-or-update depending on whether a `profiles` row exists yet, no redirect — `(app)/layout.tsx`'s onboarding lock clears via Next's automatic Server Action re-render instead) and `updateCity` (plain update, keeps the existing `redirect("/")`), and split `validateProfileInput` (`src/lib/profile/validateProfile.ts`) into matching `validateUsername`/`validateCity` functions. Gate `ProfileForm`'s city section on `Boolean(initialProfile)`, since `updateCity` can no longer create the row itself — a brand-new account sees the repurposed "Pick a username to start using PodFinder." alert in its place until their first username save creates the row.
36. Replace the Profile screen's Win Rate/Current Streak placeholder stat cards with real data, per Section 3's new RPC callout and Section 5's Screen 1: add `get_profile_pod_stats()` (`supabase/sql/profile_pod_stats.sql`), a SECURITY DEFINER RPC mirroring `get_games_played_count()`'s host-or-member/`hidden_by`-ignoring criteria, returning `pods_hosted`/`irl_count`/`online_count` in one row. Fetch it in `profile/page.tsx`'s existing `Promise.all`, and update `ProfileStats` to render four real cards (Games Played, Pods Hosted, IRL Games, Online Games) in a 2x2 grid instead of one real card plus two placeholders — extract the count-up animation into a shared `useCountUp` hook so all four animate identically. The full-width trends/graph placeholder card is unchanged.
37. Add `NoUsersFoundDialog`, per Section 3's Inactive Pod Cleanup subsection and Section 5's My Pod Panel: `MyPodPanel` shows it once a host's own pod has zero `pod_joins` and its `expires_at` has already lapsed, offering **Unlist Pod** (`cancelPod`) or **Keep Searching** (the new `extendPodSearch` server action, `src/app/actions/pods.ts`, bumping `expires_at` to `now() + 4 hours`). No new column/migration — the trigger reuses the existing `expires_at`/`pod_joins` data `OwnPodPanel` already fetches.
38. Add the platform activity trust signal, per Section 3's new callout, Section 2's Pre-auth activity signal bullet, and Section 5's Screen 3: `get_platform_activity_stats()`/`get_recent_platform_activity()` (`supabase/sql/platform_activity_stats.sql`), the first RPCs in this schema granted to `anon` as well as `authenticated`. Add `src/app/actions/platformActivity.ts` (no auth required, fails soft to `null`/`[]`) and `PlatformActivityTicker` (`src/components/PlatformActivityTicker.tsx`, polling every 45s rather than using Realtime, per the delivery-reliability note above), rendering real "N active now · M matched today" counts plus a rotating recent-event line, sourced entirely from first-party data. Mount it in `MatchFeedList`'s empty state and in `LoginView` (seeded server-side from `/login/page.tsx` for instant paint). Drop the previously-considered "fetch SpellTable's active tables" idea — no public API exists and Wizards of the Coast's terms prohibit scraping — this ticker is the first-party replacement.
39. Remove `ProfileForm`'s "Save Profile" button (per Section 5's Screen 1 City Field bullet): `CitySelector`'s `onChange` now dispatches `updateCity` directly on every selection or clear, with a `CircularProgress`/`Check` status indicator next to the field reflecting saving/saved, and an error `Alert` on failure. `updateCity` no longer ends with `redirect("/")` — it revalidates `/`, `/pods`, and `/profile` and returns an empty success state, since the user stays on `/profile` (the button and the redirect were both leftovers from before username editing moved into `ProfileHeader`, when city was still part of a single combined submit). The "no city set" info `Alert` switched from reading the static `initialProfile` prop to the form's live `city` state, so it now disappears the instant a city is picked rather than needing the old redirect-driven remount.
40. Replace the Profile screen's "Pods Hosted" stat card with "People Met", per Section 3's `get_profile_pod_stats()` callout and Section 5's Screen 1: patch `get_profile_pod_stats()` (`supabase/sql/profile_pod_stats_people_met.sql`, a `create or replace function` on top of `profile_pod_stats.sql`) to return `people_met` instead of `pods_hosted` — a distinct count of `profiles.id` across every `pod_history` row the caller appears in as host or member, excluding the caller's own id, de-duplicated by id rather than `discord_handle` since a handle can change between matches. Update `profile/page.tsx`'s fallback/prop and `ProfileStats`' prop/icon (`Crown` → `Users`) accordingly, and add the `profileStats.peopleMet` i18n key (en/fr) in place of `profileStats.podsHosted`.
41. Add the public profile side panel, per Section 3's parameterized-RPC callout and Section 5's new Public Profile Side Panel subsection: patch both stat RPCs to accept `p_user_id uuid default auth.uid()` (`supabase/sql/public_profile_stats.sql`, dropping and recreating each since a parameter is a new signature); add `fetchPublicProfile` (`src/lib/profile/fetchPublicProfile.ts`); add the presentational `UserProfilePanel` (`src/components/UserProfilePanel.tsx`, a right-anchored drawer reusing `ProfileStats`) plus `UserProfilePanelProvider`/`useUserProfilePanel()` (`src/lib/UserProfilePanelContext.tsx`, mounted in `(app)/layout.tsx`) as a global overlay syncing the URL via the raw History API rather than `next/navigation`; add the `/profile/[username]/page.tsx` route (this app's second dynamic route) plus `StandaloneProfilePanel` for direct/fresh visits; wire `openUserProfile` into `PodDetailDialog`/`PodHistoryDetailDialog` (with `onExitComplete`-based close-then-open sequencing) and directly into `MatchFeedList`/`HistoryEntriesList`/`MyPodPanel`. Add the `userProfilePanel.close`/`userProfilePanel.viewProfile` i18n keys (en/fr).
42. Turn `PlatformActivityTicker` into an always-on Pods-screen banner instead of a Login-screen/empty-state card, per Section 5's Screen 3 and Section 3's platform-activity callout: drop its `initialStats`/`initialEvents` seed props (it now always fetches client-side on mount) and redesign its markup into a full-width, single-line bar (icon, stats, and the rotating event all on one row, ellipsis-truncated, cross-faded instead of sliding) with a dismiss `IconButton`; mount it as the first child of `PodsView` (above `OwnPodPanel`/`MatchFeed`, flush against `TabBar` with no gap) instead of inside `MatchFeedList`'s empty state; remove it from `LoginView`/`login/page.tsx` entirely (dropping the latter's server-side RPC prefetch). Add the `platformActivity.dismissAria` i18n key (en/fr).
43. Add `pods.reserved_slots`, per Section 3's schema and Section 5's Search dialog subsection: `supabase/sql/add_pods_reserved_slots.sql` adds `reserved_slots INT NOT NULL DEFAULT 0 CHECK (reserved_slots >= 0 AND reserved_slots <= max_players - 2)` to `pods`, letting a host mark seats already filled by people outside the app. Add the `reservedSlots` field to `StartSearchInput`/`validateStartSearchInput` (`src/lib/pods/validateStartSearch.ts`) with the same bound, and thread it through `createPod`/`updatePod` (`src/app/actions/pods.ts`) — `updatePod`'s existing accepted-group-size guard now also counts `reserved_slots`. Add the Already Filled `ToggleButtonGroup` to `LfgDialog.tsx` next to the Players Needed stepper, and fold `reserved_slots` into every `{count}/{max} players`/`isFull` derivation in `MyPodPanel.tsx`, `PodDetailDialog.tsx`, and `MatchFeedList.tsx`. Add the shared `ReservedSlotAvatar` component (`src/components/ReservedSlotAvatar.tsx`) — a dashed-outline, tooltipped placeholder avatar — rendered once per reserved seat in My Pod Panel and `PodDetailDialog`'s member lists. Add the `lfgDialog.reservedSlotsLabel`/`reservedSlotsHelper`, `podDetailDialog.reservedSlotTooltip`, and `errors.reservedSlotsRange` i18n keys (en/fr).
44. Add the Organiser MVP (recurring weekly tables for local game stores), per Section 3's new Organiser Tables subsection and Section 5's new Screen 5: add `organizers`/`recurring_tables` tables + RLS, `pods`/`pod_history` column additions (`recurring_table_id`/`auto_accept`/`store_name`), `next_recurring_occurrence()`/`spawn_due_recurring_pods()`, `snapshot_recurring_pod_history()`, and two new `pg_cron` jobs (`spawn-recurring-table-pods`, `expire-past-recurring-pods`) — plus patches to `enforce_pod_creation_cooldown()`, `notify_on_pod_join_insert()`, and `sweep-inactive-active-pods` (`supabase/sql/recurring_tables.sql`). Add `requireOrganizerProfile()`/`getOrganizer()` (`src/lib/session.ts`), the `src/app/actions/organizer.ts` action surface, `src/lib/organizer/validateRecurringTable.ts`/`validateOrganizerProfile.ts`, the unlisted `/organizer/apply` onboarding route, and the guarded `(dashboard)` route group (`/organizer`, `/organizer/history`, `/organizer/settings`) with its `OrganizerSubNav` and `src/components/organizer/*` components. Scope every pre-existing "one active pod per user" check (`createPod`, `requestJoin`, `fetchOwnPodData`, `LfgButton`) to `recurring_table_id IS NULL`, and add the auto-accept branch to `requestJoin`. Add the amber "Organizer" `TabBar` tab (conditional on `isOrganizer`) and the "Store Event" badge / "Instant Join" treatment in `MatchFeedList`/`PodDetailDialog`.
45. Add manual session locking for recurring tables, per Section 3's Organiser Tables subsection and Section 5's Screen 5: `supabase/sql/pod_lock.sql` adds `pods.locked_at TIMESTAMP WITH TIME ZONE`; add `toggleLockPod` (`src/app/actions/pods.ts`, same `user_id`/`status = 'ACTIVE'` ownership scoping as `cancelPod`, fully reversible unlike it); add the `locked_at` check to `isPodJoinable` (`src/lib/pods/joinRules.ts`) and select it in `requestJoin` (`src/app/actions/joins.ts`). Add the Lock/Unlock button and "Locked" badge to `OrganizerRecurringTableCard.tsx`, disable/relabel `PodDetailDialog`'s join button when locked, and add a "Locked" badge to `MatchFeedList`'s card. Add the `errors.podLocked`/`errors.podLockToggleFailed` and `organizer.table.lockSession`/`unlockSession`/`locked` i18n keys (en/fr). App-layer enforced only, no RLS change.
46. Remove the recurring-table pause feature, per Section 3's schema/RLS and Section 5's Screen 5 (both updated in place, not just here): organisers now stop a table by deleting it (`deleteRecurringTable`) instead of pausing it — recreating is the only way to bring it back. `supabase/sql/remove_recurring_table_pause.sql` patches `recurring_tables.sql` (already applied to prod/staging, so edited via a new script rather than in place): drops `recurring_tables_select_active_or_own` and `recurring_tables.is_active`, adds an unconditional `recurring_tables_select_all` SELECT policy, and `create or replace`s `spawn_due_recurring_pods()` without the `is_active` filter. Remove `setRecurringTableActive` (`src/app/actions/organizer.ts`), the Pause/Play `IconButton` and `is_active`-driven dimming from `OrganizerRecurringTableCard.tsx`, `RecurringTable.is_active` (`src/types/database.ts`), and the `organizer.table.pause`/`resume` i18n keys (en/fr). Distinct from and does not touch item 45's pod-level lock feature.
47. Fix the recurring-table/ad hoc timezone bug and add end-time display, per Section 3's schema and Organiser Tables subsection and Section 5's Screen 3/Screen 5: add `recurring_tables.timezone TEXT NOT NULL DEFAULT 'Europe/Paris'` and rewrite `next_recurring_occurrence()`/`spawn_due_recurring_pods()` to convert `start_time`/`end_time` via that column instead of a hardcoded `'UTC'` (`supabase/sql/fix_recurring_table_timezone.sql`, patching already-applied `recurring_tables.sql`/`remove_recurring_table_pause.sql`, not yet applied to staging/prod). Add new shared helper `zonedTimeToUtc` (`src/lib/timezone.ts`, DST-aware via the `Intl.DateTimeFormat` round-trip idiom) and use it in `validateStartSearchInput` in place of naive `new Date()` parsing. Both `RecurringTableInput`/`validateRecurringTableInput` and `StartSearchInput`/`validateStartSearchInput` gain a `timezone: string` field, captured silently via `Intl.DateTimeFormat().resolvedOptions().timeZone` in `OrganizerRecurringTableFormDialog.tsx`/`LfgDialog.tsx` respectively and threaded through `createRecurringTable`/`updateRecurringTable` (`src/app/actions/organizer.ts`). Add `formatPodEndTime` (`src/lib/date.ts`) and show it — gated on `pod.store_name != null` — appended after the start-time label on `MatchFeedList`'s card and as a new "Ends" row in `PodDetailDialog`. Add the `podDetailDialog.endsAt` i18n key (en/fr).
48. Fix the recurring-table cancel-respawn bug, per Section 3's Organiser Tables subsection (both the schema index and the new "Cancelling a session" bullet, updated in place) and Section 5's Screen 5: `supabase/sql/fix_recurring_table_cancel_respawn.sql` (not yet applied to staging/prod) drops `pods_one_active_per_recurring_occurrence` and replaces it with `pods_one_per_recurring_occurrence` on `pods(recurring_table_id, scheduled_at) WHERE recurring_table_id IS NOT NULL` — no `status` filter, so a manually cancelled occurrence (`cancelPod`) can never be re-spawned by the next `spawn-recurring-table-pods` run, unlike before. No changes to `spawn_due_recurring_pods()` or `cancelPod` themselves. Add a same-session info message to `OrganizerRecurringTableCard.tsx` after a successful cancel (new `justCancelled` state, cleared once `fetchUpcomingPod` finds a pod again), reusing `dayOfWeekLabel`/`startTimeLabel` (`src/lib/date.ts`) to tell the organiser the table will spawn again next `{day}` at `{time}`. Add the `organizer.table.cancelledInfo` i18n key (en/fr).
49. Add a "Copy link" action to `OrganizerRecurringTableCard.tsx`'s session button row, per Section 5's Screen 5: writes `${window.location.origin}/pods/${pod.id}` (the currently-spawned pod's own `/pods/[id]` route, already used by `MyPodPanel`'s host share link) to the clipboard via `navigator.clipboard.writeText`, relabelling the button "Link copied" (with a `Check` icon swap) for two seconds before reverting — a local `linkCopied` boolean, no new route, server action, or schema change. Add the `organizer.table.copyLink`/`linkCopied` i18n keys (en/fr).
50. Replace organizer onboarding's self-serve `organizers` insert with an admin-reviewed application flow, per Section 3's new Admin Review subsection and Section 5's new Screen 6: `supabase/sql/organizer_application_review.sql` adds `profiles.is_admin` (manually granted), the `organizer_applications` table + RLS (`pending`/`approved`/`rejected`, one pending row per user), drops `organizers_insert_self` (closing the direct self-insert path), adds two `notification_type` values (`ORGANIZER_APPLICATION_APPROVED`/`REJECTED`), and `approve_organizer_application`/`reject_organizer_application` (SECURITY DEFINER RPCs — approve upserts into `organizers` and notifies the applicant in-app, reject just notifies). Add `requireAdminProfile()` (`src/lib/session.ts`), `src/app/actions/admin.ts`, and the guarded `/admin/applications` route (`src/app/(app)/admin/`) with `AdminApplicationList`/`AdminApplicationCard`/`AdminApplicationDetailDialog` (`src/components/admin/`, mirroring `OrganizerRecurringTableList`/`PodDetailDialog`'s existing patterns). Add the violet "Admin" `TabBar` tab (conditional on `isAdmin`). Add an `email` field to `OrganizerApplyForm`/`applyAsOrganizer` (new `src/lib/organizer/validateOrganizerApplication.ts`, wrapping the existing `validateOrganizerProfileInput`) and a "pending review" state to `/organizer/apply` for an applicant with an already-`pending` row. Add `notification.organizerApplicationApproved`/`Rejected`, `admin.applications.*`, `organizer.apply.email`/`pendingTitle`/`pendingSubtitle`, `tabBar.admin`, and `errors.alreadyApplied`/`emailRequired`/`emailInvalid`/`applicationReviewFailed` i18n keys (en/fr). No email-sending integration was added — "notify the applicant" is the in-app notification only.
51. Fix two bugs in step 50's admin review flow, per Section 3's Admin Review subsection (updated in place): `src/app/(app)/admin/applications/page.tsx`'s `organizer_applications` query used an ambiguous `.select("*, profiles(...)")` embed — the table has two FKs to `profiles` (`user_id`, `reviewed_by`) — so PostgREST returned an error instead of data, silently swallowed into an empty list (submitted applications never appeared on `/admin/applications`); fixed with an explicit `profiles!organizer_applications_user_id_fkey(...)` embed, plus logging the query's `error` going forward. Also add a submission receipt: `supabase/sql/fix_organizer_application_notify_submitted.sql` adds `ORGANIZER_APPLICATION_SUBMITTED` to `notification_type` and `notify_on_organizer_application_insert()` (SECURITY DEFINER, `after insert on organizer_applications`), notifying the applicant themselves the moment they submit. Wire the new type through `NotificationBell`'s `TYPE_ICON`, `NotificationCenterProvider`'s `describeNotification`/`getNotificationHref` (routes to `/organizer/apply`, same as the rejected case), and add the `notification.organizerApplicationSubmitted` i18n key (en/fr).
52. Attempt (twice) to make the "Organizer" tab appear automatically after acceptance, then abandon it in favor of telling the user to reload, per Section 3's Admin Review subsection and Section 5's Navigation Shell (both updated in place). `isOrganizer` is computed once per request in `(app)/layout.tsx` and passed to `TabBar` as a plain prop, cached client-side by `staleTimes.dynamic` (`next.config.ts`), so an already-open tab kept serving `isOrganizer: false` after acceptance. First attempt: pair `router.refresh()` with the existing `router.push()` calls — unreliable (the push could navigate before the refresh's invalidation landed). Second attempt: lift `isOrganizer` into client state on `NotificationCenterProvider.tsx` (an `initialIsOrganizer` prop seeding a `useState`, flipped to `true` by `handleNewNotification` on `ORGANIZER_APPLICATION_APPROVED`, read by `TabBar.tsx` via `useNotificationCenter()` instead of a prop) — still didn't reliably surface the tab in testing. Both reverted; `isOrganizer`/`TabBar` are back to the original plain-prop shape. Final fix: `notification.organizerApplicationApproved` (i18n, en/fr) now tells the applicant to reload the page to see the tab. `isAdmin` was never touched — still a plain server-passed prop throughout, since nothing grants it via a live notification. No schema or new dependency.
```

If you have questions at any point, ask me directly.
