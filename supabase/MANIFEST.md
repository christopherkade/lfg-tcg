# Supabase script manifest

This project's Supabase schema is **not** tracked by the Supabase CLI / migrations
tooling. Instead, `schema.sql` is the baseline DDL and everything under `sql/` is a
one-off script applied by hand via the Supabase SQL editor. This file is the record
of what's been applied where, in what order, so a second project (e.g. staging) can
be brought to parity and kept there.

This manifest tracks **apply history only** — not architecture. Schema, RLS, and
trigger design live in [docs/specs/03-schema.md](../docs/specs/03-schema.md) (SPECS.md
§3), and any change that adds/alters schema, RLS, triggers, `GAMES_CONFIG`, server
actions, routes, or screens must still go through the `update-specs` skill to keep
those docs current, in addition to getting a row here.

## Rule for new scripts

1. Add a row below before writing the script.
2. Apply to **staging** first via its SQL editor, smoke-test the affected feature.
3. Apply the identical file to **prod**.
4. Tick both boxes.

Never apply a new script to prod before it's been verified on staging.

## Applied scripts

| Order | File                                           | Prod applied   | Staging applied | Notes                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             |
| ----- | ---------------------------------------------- | -------------- | --------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1     | `schema.sql`                                   | [x] 2026-07-27 | [x]             | Baseline DDL. Has `DROP ... IF EXISTS` guards — only safe to run in full against an **empty** project.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 2     | `sql/account_deletion.sql`                     | [x] 2026-07-27 | [x]             |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 3     | `sql/inactive_pod_cleanup.sql`                 | [x] 2026-07-27 | [x]             | Adds a `notification_type` enum value and uses it later in the same file — run statement 1 alone first, then the rest. Also schedules a `pg_cron` job; verify with `select * from cron.job;`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                     |
| 4     | `sql/matched_notification_seen.sql`            | [x] 2026-07-27 | [x]             |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 5     | `sql/matched_pod_cleanup.sql`                  | [x] 2026-07-27 | [x]             | Schedules a `pg_cron` job; verify with `select * from cron.job;`.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 6     | `sql/rate_limits.sql`                          | [x] 2026-07-28 | [x]             |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 7     | `sql/notify_pending_joiners_on_pod_update.sql` | [x] 2026-07-29 | [x]             | Adds a `notification_type` enum value and uses it later in the same file — run statement 1 alone first, then the rest.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 8     | `sql/notify_pod_destroyed.sql`                 | [x] 2026-07-29 | [x]             | Adds a `notification_type` enum value and uses it later in the same file — run statement 1 alone first, then the rest.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                            |
| 9     | `sql/pod_history.sql`                          | [x] 2026-07-29 | [x]             |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 10    | `sql/scale_indexes.sql`                        | [x] 2026-07-29 | [x]             |                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                   |
| 11    | `sql/fix_pod_join_delete_notify_actor.sql`     | [x] 2026-07-29 | [x]             | `create or replace function` patch on `notify_on_pod_join_delete` (defined in `schema.sql`) — just needs to run after the baseline, no other ordering dependency.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                 |
| 12    | `sql/profile_pod_stats.sql`                    | [x] 2026-07-30 | [x]             | Adds `get_profile_pod_stats()` only — no table/RLS change. Depends on `pod_history` existing (order 9).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 13    | `sql/platform_activity_stats.sql`              | [x] 2026-07-31 | [x]             | Adds `get_platform_activity_stats()`/`get_recent_platform_activity()` + 2 indexes. **Both functions are granted to `anon`, not just `authenticated`** — the first such grant in this schema (needed for the pre-auth Login page ticker); see docs/specs/03-schema.md's new callout before applying. Depends on `pod_history` existing (order 9).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                  |
| 14    | `sql/profile_pod_stats_people_met.sql`         | [x]            | [x]             | `drop function` + recreate patch on `get_profile_pod_stats()` (order 12) — replaces `pods_hosted` with `people_met` (distinct `profiles.id` across `pod_history` host/members, excluding self). The column set changed so `create or replace` alone fails with `42P13`; the script drops the old function first. Depends on `pod_history` existing (order 9). **Superseded by order 15 below** — skip this one if not yet applied.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                |
| 15    | `sql/public_profile_stats.sql`                 | [x] 2026-07-31 | [x]             | Drops and recreates `get_profile_pod_stats()`/`get_games_played_count()` with an optional `p_user_id uuid default auth.uid()` param, so the public profile side panel can fetch another user's stats. Fully supersedes order 14 (same `people_met` body, plus the new param) — apply this instead if 14 isn't applied yet.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                        |
| 16    | `sql/add_pods_reserved_slots.sql`              | [x] 2026-08-01 | [ ]             | Adds `pods.reserved_slots INT NOT NULL DEFAULT 0 CHECK (reserved_slots >= 0 AND reserved_slots <= max_players - 2)` — seats a host has already filled with people outside the app. Landed as a hotfix directly on `main` (bypassing `staging`/this manifest) alongside `src/components/ReservedSlotAvatar.tsx`; backfilled into the manifest and merged back into `staging` on 2026-08-04 per `AGENTS.md`'s hotfix flow. **Apply to staging's SQL editor before relying on this column there** — it is live on prod but was never run against staging's Supabase project.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                         |
| 17    | `sql/recurring_tables.sql`                     | [x] 2026-08-04 | [x]             | Adds the Organiser feature: `organizers`/`recurring_tables` tables + RLS, `pods`/`pod_history` column additions, `spawn_due_recurring_pods()` + two new `pg_cron` jobs (`spawn-recurring-table-pods`, `expire-past-recurring-pods`; verify with `select * from cron.job;`). Also **patches 3 earlier functions/jobs**: `enforce_pod_creation_cooldown()` (order 6), `notify_on_pod_join_insert()` (`schema.sql`), and the `sweep-inactive-active-pods` job (order 3) — re-running those scripts after this one would clobber the patches, so always apply in numeric order on a fresh project. Depends on `pod_history` existing (order 9) and `rate_limits.sql` existing (order 6). **Has its own reset block (step 0) dropping everything it creates/touches first**, so — unlike every other script here — it's safe to re-run from scratch after a partial failure; remove that block once this feature has real data in some environment.                                                                                                                                                                    |
| 18    | `sql/fix_pod_joins_delete_host.sql`            | [x] 2026-08-04 | [x]             | Adds the missing `pod_joins` DELETE policy letting a pod's **host** remove an accepted member's join row (`removeMember`, `src/app/actions/joins.ts`) — until now `pod_joins` had only `pod_joins_delete_own` (`schema.sql`), so a host clicking "Remove" (in `MyPodPanel` or `OrganizerRecurringTableCard`) silently deleted 0 rows via RLS, no error. Pre-existing bug, unrelated to `recurring_tables.sql` — just surfaced by testing the organiser card. `drop policy if exists` guard, safe to re-run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 19    | `sql/pod_lock.sql`                             | [x]            | [ ]             | Adds `pods.locked_at timestamptz` — set/cleared by `toggleLockPod` (`src/app/actions/pods.ts`) so an organizer can close new joins on a recurring table's live session without cancelling it, then reopen. App-layer enforced only (checked in `isPodJoinable`, `src/lib/pods/joinRules.ts`), no RLS change — consistent with `status` not being RLS-enforced either. `add column if not exists` guard, safe to re-run.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                           |
| 20    | `sql/remove_recurring_table_pause.sql`         | [x]            | [ ]             | Removes the recurring-table "pause" feature (organisers now stop a table via delete instead) — patches order 17 (already applied, so can't be safely re-applied from scratch): drops `recurring_tables_select_active_or_own`, drops `recurring_tables.is_active`, adds an unconditional `recurring_tables_select_all` SELECT policy, and `create or replace`s `spawn_due_recurring_pods()` without the `is_active` filter. Unrelated to `sql/pod_lock.sql`'s pod-level lock feature — that stays untouched.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                       |
| 21    | `sql/fix_recurring_table_timezone.sql`         | [x]            | [ ]             | Fixes the recurring-table timezone bug: `next_recurring_occurrence()`/`spawn_due_recurring_pods()` explicitly treated naive `start_time`/`end_time` as UTC, shifting every spawned pod's `scheduled_at`/`expires_at` for any organiser outside UTC. Adds `recurring_tables.timezone text not null default 'Europe/Paris'` (existing rows backfilled to that default — an organiser outside metropolitan France must re-save their table once to pick up their real zone) and re-derives both functions to convert using that column instead of a hardcoded `'UTC'`. `next_recurring_occurrence` gains a `_timezone` param (signature change — drops the old 2-arg overload first). Depends on order 20 already being applied (assumes its `is_active`-free loop body).                                                                                                                                                                                                                                                                                                                                            |
| 22    | `sql/fix_recurring_table_cancel_respawn.sql`   | [x]            | [ ]             | Fixes a bug where cancelling a recurring table's spawned pod ahead of its start time (`cancelPod`, which only sets `status = 'EXPIRED'`) got silently undone by the next hourly `spawn-recurring-table-pods` run, since the old `pods_one_active_per_recurring_occurrence` index only blocked a second **ACTIVE** row for the same occurrence. Drops that index and replaces it with `pods_one_per_recurring_occurrence` on `(recurring_table_id, scheduled_at) where recurring_table_id is not null` (no `status` filter) — one pod per occurrence ever, regardless of status. **Step 0 first deletes pre-existing duplicate `(recurring_table_id, scheduled_at)` rows** the bug already produced (`CREATE UNIQUE INDEX` fails with a `23505` otherwise, hit on first apply attempt) — keeps the `ACTIVE` row per occurrence if one exists, else the most recently created row; cascade-deletes the rest's `pod_joins`/`notifications`, `pod_history` untouched. Independent of orders 20/21 (only touches the index, not `spawn_due_recurring_pods()`'s body), safe to apply in either order relative to those. |
| 23    | `sql/fix_pod_joins_respond_pending_only.sql`   | [x]            | [ ]             | Fixes a bug where a rejected join request could still end up accepted: `respondToJoin` (`src/app/actions/joins.ts`) updated `pod_joins.status` filtered only by row id, and `pod_joins_update_host_only` (`schema.sql`) had no status predicate either — so a duplicate/retried respond call (two tabs, a slow click followed by a second one) could apply two decisions to the same row, with whichever `UPDATE` committed last winning silently and no error surfaced. Adds `pod_joins.status = 'PENDING'` to the policy's `USING` clause so only a still-pending row can be targeted at all; paired with the same `.eq("status", "PENDING")` guard added to the app-layer update. `drop policy if exists` guard, safe to re-run.                                                                                                                                                                                                                                                                                                                                                                               |
| 24    | `sql/fix_pod_joins_auto_delete_rejected.sql`   | [x]            | [ ]              | Fixes the requester having to manually click "Dismiss" (`leavePod`) to clear a `REJECTED` `pod_joins` row before they could request to join again — the row now auto-deletes itself as soon as `respondToJoin` transitions it `PENDING -> REJECTED`. Adds `auto_delete_rejected_pod_join()` (SECURITY DEFINER, same convention as `notify_on_pod_join_insert/update/delete`) plus a matching `after update ... when (old.status = 'PENDING' and new.status = 'REJECTED')` trigger; the nested `delete` doesn't affect `pod_joins_notify_update`'s already-queued `JOIN_REJECTED` notification, and can't trigger a spurious `MEMBER_LEFT` since `pod_joins_notify_delete` only fires `when (old.status = 'ACCEPTED')`. `create or replace function` + `drop trigger if exists` guards, safe to re-run. Independent of order 23 (that one guards the UPDATE itself; this one reacts after it succeeds).                                                                                                                |
| 25    | `sql/organizer_application_review.sql`        | [x]            | [ ]              | Replaces organizer onboarding's "unlisted URL is the only gate" model with real admin review: adds `profiles.is_admin` (manually granted to `discord_handle = 'counterspell_fr'`), a new `organizer_applications` table (status pending/approved/rejected, RLS: applicant sees/inserts own, admin sees all, no client update policy), two new `notification_type` enum values, and `approve_organizer_application`/`reject_organizer_application` SECURITY DEFINER RPCs (admin-check inside; approve inserts into `organizers`, both insert a `notifications` row). Enum values must be added in their own statement/transaction first — see the script's own header. Depends on `organizers` existing (order 17). |
| 26    | `sql/fix_organizer_application_notify_submitted.sql` | [x]            | [ ]              | Patches order 25 (already applied): adds `ORGANIZER_APPLICATION_SUBMITTED` to `notification_type` and a `notify_on_organizer_application_insert()` SECURITY DEFINER trigger (`after insert on organizer_applications`) that notifies the applicant themselves as a submission receipt. Also fixes an app-code bug (no schema change): `/admin/applications`'s query used an ambiguous `profiles(...)` embed — `organizer_applications` has two FKs to `profiles` (`user_id`, `reviewed_by`) — which silently returned zero rows; `src/app/(app)/admin/applications/page.tsx` now uses the explicit `profiles!organizer_applications_user_id_fkey(...)` embed. Enum value must be added in its own statement/transaction first — see the script's own header. Depends on order 25. |

Dates above are the file's last-modified date at the time this manifest was
created (2026-07-29), used as a best-effort proxy for real apply order/date since
no prior tracking existed.
